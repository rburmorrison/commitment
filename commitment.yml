cargo-build:
  execute:
    - cargo clean
    - cargo build --color=always

cargo-test:
  execute:
    - cargo fmt --check

cargo-fmt:
  execute:
    - cargo fmt --check

cargo-clippy:
  execute:
    - cargo clippy --color=always -- -D warnings
